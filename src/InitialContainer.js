import React from 'react';
import App from './containers/App.js';

// eslint-disable-next-line
export default class InitialContainer extends React.PureComponent {
  render() {
    return <App />;
  }
}
